'use strict';

/**
 * teacher-training-course-delta router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::teacher-training-course-delta.teacher-training-course-delta');
